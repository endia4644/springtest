services:
  app:
    image: ghcr.io/${GITHUB_REPOSITORY}:latest
    restart: always
    expose:
      - "8080"

  nginx:
    image: nginx:alpine
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
